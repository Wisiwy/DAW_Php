<?php
/*$nombre = "Raul";
$array = [1, 2, 3, 4, 5, 6];
var_dump($array); //vuelca en memoria la variable. Debug. Ver cuanto vale una variable. Son usadas.
//Instalar xdebug sudo apt update && sudo

//FUNCIONES
//si me llega una variable (leer formularios

if (empty($valor)) {
    echo empty($valor) ? //true si variable vacia, 0 o false devuelve vacia.
        $valor = 0;
    echo empty($valor)?
echo empty($valor) ?
echo empty($valor)  ;
}

*/?>